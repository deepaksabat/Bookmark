module BookMarksHelper
	#def label_cloud(labels)#, classes)
    #max = labels.collect(&:count)
   # labels.each do |label|
    	#index = label.count.to_i
      #index = label.count.to_f / max.count * (classes.size - 1)
     #yield(label)#, classes[index.round])
   #end
  #end
end
